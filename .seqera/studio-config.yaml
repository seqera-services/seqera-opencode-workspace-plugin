schemaVersion: "0.0.1"
kind: "studio-config"
session:
  name: "opencode-studio"
  description: "Seqera Studio image that runs opencode serve behind connect-client."
  template:
    kind: "dockerfile"
    dockerfile: "Dockerfile"
